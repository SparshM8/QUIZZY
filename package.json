{
  "name": "quizzy",
  "version": "0.1.0",
  "private": true,
  "description": "QUIZZY \u2014 assessment, recruitment and analytics platform (monorepo: frontend + backend)",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend && npm run vercel:function-build --workspace=backend",
    "test": "npm run test --workspace=backend",
    "dev": "npm run dev --workspace=backend"
  },
  "packageManager": "npm@10.9.2"
}